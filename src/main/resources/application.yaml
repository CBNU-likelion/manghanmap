spring:
  datasource:
    url: jdbc:mysql://localhost:3306/manghanmap?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: yeontteo
    password: namys0103!m
    driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080